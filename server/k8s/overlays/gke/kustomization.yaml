apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - ../../base/

resources:
  - namespace.yaml

# Contain everything in a namespace to avoid name collisions
namespace: trace-together
